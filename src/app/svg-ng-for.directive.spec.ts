import { SvgNgForDirective } from './simple-ng-for.directive';

describe('SimpleNgForDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgNgForDirective();
    expect(directive).toBeTruthy();
  });
});
